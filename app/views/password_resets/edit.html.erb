<%= provide(:title, "Password Reset") %>
<h1>Password Reset</h1>

<div class="row">
  <div>
    <%= form_for(@user, url: password_reset_url(params[:id])) do |f| %>
      <%= render "/shared/error_messages" %>

      <!-- we need to find the user by email (in edit & update in ctrler) which is available in the link
      but the as soon as the form is sent the value will be lost -->
      <!-- hidden field solves this problem by placing (but not displaying) the email on the page,
      and then submitting it along with the rest of the form’s information -->
      <!-- hidden field tag => params[:email -->
      <!-- hidden field => params[:user][:email] -->
      <%= f.hidden_field_tag :email, @user.email %>

      <%= f.label :password %>
      <%= f.password_field :password, class: "form-control" %>

      <%= f.label :password_confirmation, "Password Confirmation" %>
      <%= f.password_field :password_confirmation, class: "form-control" %>

      <%= f.submit "Update Password", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>