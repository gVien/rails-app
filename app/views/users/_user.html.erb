<li>
  <%= gravatar_for(user, size: 50) %>
  <%= link_to user.name, user %>
  <!-- what the right condition does is that it does not allow admin to delete itself. to understand it better, write the conditions out for a few users -->
  <% if current_user.admin? && !current_user?(user)   #check if current user is admin & check if user is not current user   %>
    | <%= link_to "delete", user,  method: :delete, data: { confirm: "Proceed with deleting: #{ user.name }?"} %>
  <% end %>
</li>